#ifndef HANDLE_PRINTS
#define HANDLE_PRINTS
#include "activate_grep_structure.h"
void handle_lines_prints(char const *line, arguments_struct *my_grep, int num_line, int num_bytes, char *sign);
#endif
